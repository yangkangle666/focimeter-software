# 原始 C++ 原型

`focimeter_cpp/` 是现有 Visual Studio + OpenCV 工程，用作项目理解和答辩依据。

它包含图像处理、坐标系和计算代码，但不等同于新的四模块开发结构。各组开发应在 `focimeter_system/modules/` 下进行，并遵守统一接口。

`focimeter_article_source_snapshot.cpp` 是外部文章附带代码的原样快照，不参与构建。来源和使用边界见 `ARTICLE_SOURCE.md`。
