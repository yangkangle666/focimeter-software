# 第二阶段任务看板

更新时间：2026-08-13  
当前集成基线：`develop@db8c815`

## 总体顺序

1. M2 修复第二阶段真实数据首轮盲测暴露的候选生成/背景过滤问题。
2. M2 修复后重新运行全部 12 个真实输入包。
3. 只有 M2 产生有效输出后，M3 才继续做跨图匹配、S/C/A 和重复性验证。
4. 若 M3 出现新失败，按实际日志判断责任并单独派发 M3 任务。
5. M1、M4 当前只做指定范围内的复核或准备，不得为了“先跑通”修改其他模块。
6. 第二阶段验证通过后，再启动 M4 串联、界面和本地程序回归，最后才考虑 `main`。

## 当前任务

| 模块 | 状态 | 当前任务 | 允许修改范围 | 交付与验收 |
| --- | --- | --- | --- | --- |
| M1 输入与配置 | 待命/只读复核 | 核对 12 个真实输入包、配置枚举、路径和 manifest；发现明确 M1 错误才报告 | `focimeter_system/modules/input_config/`；数据/配置需负责人授权 | 12 个包输入检查结果；无问题则不创建无意义 PR |
| M2 图像识别 | **进行中，当前主任务** | 改进通用候选生成或有证据的背景预筛选，解决 `SPOT_COUNT_MISMATCH` | `focimeter_system/modules/image_recognition/` | 新分支从 `develop@db8c815` 创建；不能只提高 150 上限，不能按文件名/镜片号/坐标/S/C/A 写特例；M2 CTest `17/17` 保持通过；12 包逐组报告 |
| M3 标定与计算 | 等待 M2 输出/只读 | 暂不改代码；M2 成功后运行匹配、S/C/A、重复性和工程对照 | `focimeter_system/modules/calibration_calculation/` | 记录每组检测点数、唯一匹配数、置信度、S/C/A 和重复性；仅对实际 M3 失败提交修复 PR |
| M4 本地系统与展示 | 暂停开发/准备 | 阅读接口契约和 UI/操作参考，准备消费 M1/M2/M3 标准 JSON 的主流程设计 | `focimeter_system/modules/local_system/` | 第二阶段 M1-M3 通过后再开工；不得重写 M2/M3 算法 |

## 数据与报告

- 主数据集：`focimeter_system/data/real/multispot_lens_pairs/real_lens_repeat_set_002/`
- 首轮盲测报告：同目录 `PHASE2_BLIND_RUN_20260813.md`
- 首轮结果：M1 `12/12`；M2 `0/12`；M3 未运行。
- 已确认使用镜片 1～3，共 12 个输入包；关系不确定的第 4 组排除。
- 真实数据只用于软件联调和重复性检查，仍须保持 `software_verified`、`software_only`、`metrology_validated=false`。

## GitHub 协作要求

- 只从最新 `origin/develop` 创建一次性 `task/m1-*`、`task/m2-*`、`task/m3-*` 或 `task/m4-*` 分支，PR 目标为 `develop`。
- 不得直接在 `develop` 或 `main` 开发和推送，不得沿用已合并/已关闭的旧任务分支。
- 默认只能修改本人模块；跨模块、共享配置、接口、数据、CI 和根目录文件必须先取得负责人明确授权。
- PR 必须说明：基线提交、修改文件范围、失败原因、测试命令及逐组结果。
- 不得把工程模式输出写成计量验证成功；不确定时保守拒绝并记录证据。

## 已完成基线

- M1、M2、M3 已合入 `develop`。
- 第一阶段已有真实数据 M1 -> M2 -> M3 工程模式链路已跑通。
- 模拟回归：M1 `54/54`、M2 `17/17`、M3 `130/130`、Mock `43/43`。
- 历史 Web PR #7 暂不处理，不属于当前第二阶段任务。
